{
  "name": "frontend-version-action",
  "version": "1.0.0",
  "description": "Smart Versioning Action for frontend projects",
  "main": "index.js",
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
